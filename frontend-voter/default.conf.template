server {
    listen ${PORT};
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    # Optional: handle assets & SW
    location /assets/ {
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    location /sw.js {
        add_header Cache-Control "no-cache";
    }

    location /manifest.webmanifest {
        add_header Cache-Control "no-cache";
    }
}
